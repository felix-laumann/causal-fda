#PBS -l select=2:ncpus=8:mem=64gb

#PBS -lwalltime=72:0:0

cd $HOME/causal-fda

# Load modules
module load anaconda3/personal

# Input data and output configuration
export DATA_DIR="$HOME/"

source activate base
conda activate causal_felix

# Run program
python run_causal_tests.py --n_trials 20 --n_samples 200 --period 0.1 --test 'joint' --n_vars 6 --cd_type 'constraint' --a 1

