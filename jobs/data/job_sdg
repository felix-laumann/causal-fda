#PBS -l select=1:ncpus=32:mem=512gb

#PBS -lwalltime=72:0:0

cd $HOME/causal-fda

# Load modules
module load anaconda3/personal

# Input data and output configuration
export DATA_DIR="$HOME/"

source activate base
conda activate causal_felix

# Run program
python SDG_causal_discovery.py --cd_type 'constraint' --find_Lambda True

